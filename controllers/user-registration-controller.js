const userRegistration = (request, response) => {
  response.send(
    '<h1 style="text-align:center;color:red;">A user registration module is under development.</h1>'
  );
};

export { userRegistration };
